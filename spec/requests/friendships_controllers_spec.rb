require 'rails_helper'
=begin
RSpec.describe "FriendshipsControllers", type: :request do
  describe "GET /friendships_controllers" do
    it "works! (now write some real specs)" do
      get friendships_controllers_path
      expect(response).to have_http_status(200)
    end
  end
end
=end
