<%= content_for :title, "New entity — #{@campaign.name}" %>

<div class="container-narrow">
  <%= render partial: "layouts/flash" %>
  <h1 style="margin-bottom: 1rem;">New entity</h1>
  <div class="form-card form-card--medium">
    <%= render "form" %>
  </div>
</div>
