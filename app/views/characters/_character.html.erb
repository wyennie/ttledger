<div id="character_<%= character.id %>" class="character-card">
  <li class="character-card__row">
    <% if character.persisted? %>
      <%= link_to "#{character.name}", edit_campaign_character_path((@campaign || character.campaign), character) %>
    <% end %>
  </li>
</div>
