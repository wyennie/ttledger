<div id="chat-form"
    data-controller="chat"
    data-campaign-id="<%= @campaign.id %>"
    data-page-slug="<%= @page.slug %>"
    data-chat-target="chatForm"
    class="chat-toggle">

  <div data-chat-target="chatFormContent" class="chat-panel hidden">
    <%= render partial: "chats/show" %>
  </div>
</div>
